# Blocked Definition

`blocked=true` means lane execution failed.

Examples:
- command crashed
- auth missing
- write failed

Non-examples:
- unresolved findings
- pending future PR work
- waiting for human merge/review
